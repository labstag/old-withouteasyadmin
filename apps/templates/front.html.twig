{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('front') }}
{% endblock %}
{% block content %}
    {% for row, blockhtml in blocks %}
        <{{ row }}>
            {% for row in blockhtml %}
                {{ debug_begin(row.template) }}
                {{ row.data.content|raw }}
                {{ debug_end(row.template) }}
            {% endfor %}
        </{{ row }}>
    {% endfor %}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('front') }}
{% endblock %}
