{% extends 'admin/edito/layout.html.twig' %}

{% block title %}Edito{% endblock %}

{% block content %}
    <h1>Edito</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ edito.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ edito.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ edito.content }}</td>
            </tr>
            <tr>
                <th>Enable</th>
                <td>{{ edito.enable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('edito_index') }}">back to list</a>

    <a href="{{ path('edito_edit', {'id': edito.id}) }}">edit</a>

    {{ include('admin/edito/_delete_form.html.twig') }}
{% endblock %}
