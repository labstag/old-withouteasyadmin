<form method="post" action="{{ path('admin_lienuser_delete', {'id': lien_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lien_user.id) }}">
    <button class="btn">Delete</button>
</form>
