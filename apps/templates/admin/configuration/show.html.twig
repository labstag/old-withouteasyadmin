{% extends 'admin/configuration/layout.html.twig' %}
{% block body %}
    <table
  class="table table-bordered table-hover table-striped"
  data-locale="fr-FR"
  data-toggle="table"
  data-search="true"
  data-show-columns="true">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ configuration.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ configuration.value ? configuration.value|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_configuration_index') }}">back to list</a>

    {{ include('admin/configuration/_delete_form.html.twig') }}
{% endblock %}
