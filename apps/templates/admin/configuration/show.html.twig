{%- extends 'admin/configuration/layout.html.twig' -%}
{%- block body -%}
    <table>
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ entity.value ? entity.value|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>
{%- endblock -%}
