{% extends 'admin/configuration/layout.html.twig' %}
{% block content %}
    <h1>Configuration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ configuration.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ configuration.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('configuration_index') }}">back to list</a>

    <a href="{{ path('configuration_edit', {'id': configuration.id}) }}">edit</a>

    {{ include('admin/configuration/_delete_form.html.twig') }}
{% endblock %}
