{% extends 'admin/email_user/layout.html.twig' %}
{% block body %}
    <table
  data-locale="fr-FR"
  data-toggle="table"
  data-search="true"
  data-show-columns="true">
        <tbody>
            <tr>
                <th>Adresse</th>
                <td>{{ email_user.adresse }}</td>
            </tr>
            <tr>
                <th>Principal</th>
                <td>{{ email_user.principal ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Verif</th>
                <td>{{ email_user.verif ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td><a href="{{ path('admin_user_show', {'id': email_user.refuser.id}) }}">{{ email_user.refuser }}</a></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_emailuser_index') }}">back to list</a>

    <a href="{{ path('admin_emailuser_edit', {'id': email_user.id}) }}">edit</a>

    {{ include('admin/email_user/_delete_form.html.twig') }}
{% endblock %}
