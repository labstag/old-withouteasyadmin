{% extends 'admin/page/layout.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.slug) }}
    {{ form_row(form.parent) }}
    {{ form_row(form.front) }}
    {{ form_row(form.function) }}
    {{ form_end(form) }}
{% endblock %}
