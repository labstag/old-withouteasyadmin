{% import "admin/macro.html.twig" as macro_admin %}
{% extends 'admin/post/libelle/layout.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    {% if entity.id != null %}
        {{ macro_admin.form_img(
            entity,
            entity.fond,
            "Fond",
            path('api_attachment_editofond', {'entity': entity.id})
        ) }}
    {% endif %}
    {{ form_row(form.file) }}
    {{ form_row(form.refuser) }}
    {{ form_end(form) }}
{% endblock %}
