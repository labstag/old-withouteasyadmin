{% extends 'admin.html.twig' %}
{% block body %}
    <table>
        <tbody>
            <tr>
                <th>{{ 'template.name'|trans({}, 'admin.messages') }}</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>{{ 'template.code'|trans({}, 'admin.messages') }}</th>
                <td>{{ entity.code }}</td>
            </tr>
            <tr>
                <th>{{ 'template.html'|trans({}, 'admin.messages') }}</th>
                <td>{{ entity.html|raw }}</td>
            </tr>
            <tr>
                <th>{{ 'template.text'|trans({}, 'admin.messages') }}</th>
                <td>{{ entity.text }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
