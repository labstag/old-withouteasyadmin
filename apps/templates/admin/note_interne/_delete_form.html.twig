<form method="post" action="{{ path('note_interne_delete', {'id': note_interne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note_interne.id) }}">
    <button class="btn">Delete</button>
</form>
