{% import "admin/macro.html.twig" as macro_admin %}
{% extends 'admin/note_interne/layout.html.twig' %}
{% block body %}
    <table
        is="table-datatable"
        data-action="{{ "table.admin.action"|trans|e('html_attr') }}"
        data-multiple
        data-multipleall="{{ "table.admin.multipleall"|trans|e('html_attr') }}"
        data-multipleelement="{{ "table.admin.mutipleelement"|trans|e('html_attr') }}"
        data-empty="{{ "table.admin.empty"|trans|e('html_attr') }}"
        data-count={{ pagination|length }}
    >
        <thead>
            <tr>
                <th>{{ "noteinterne.title"|trans }}</th>
                <th>{{ "noteinterne.date_debut"|trans }}</th>
                <th>{{ "noteinterne.date_fin"|trans }}</th>
                <th>{{ "noteinterne.user"|trans }}</th>
                <th>{{ "admin.workflow.state"|trans }}</th>
                <th>{{ "noteinterne.workflow"|trans }}</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr data-id="{{ entity.id }}">
                <td>{{ entity.title }}</td>
                <td>{{ entity.dateDebut ? entity.dateDebut|format_datetime(pattern=formatdatetime.admin) : '' }}</td>
                <td>{{ entity.dateFin ? entity.dateFin|format_datetime(pattern=formatdatetime.admin) : '' }}</td>
                <td>{{ entity.refuser }}</td>
                <td>
                    {% for state in workflow_marked_places(entity) %}
                        {{ state }}
                    {% endfor %}
                </td>
                <td>
                    {{ macro_admin.workflow(entity, actions) }}
                </td>
                <td>
                    {{ macro_admin.actions(entity, actions) }}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
