{% extends 'admin/menu/layout.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {% if form.clef is defined %}
        {{ form_row(form.clef) }}
    {% else %}
        {{ form_row(form.name) }}
        {{ form_row(form.icon) }}
    {% endif %}
    {{ form_end(form) }}
{% endblock %}
