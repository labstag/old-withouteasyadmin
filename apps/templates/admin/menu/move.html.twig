{% import "admin/menu/macro.html.twig" as macro_menu %}
{% extends 'admin/menu/layout.html.twig' %}
{% block body %}
    <div class="row">
        <div class="col-6">
            <ul is="entity-move">
            {% for child in menu.children %}
                <li data-id="{{ child.id }}">
                    {% if child.separateur %}
                        {{ 'menu.divider'|trans({}, 'admin.messages') }}
                    {% else %}
                        {{ child.name }}
                    {% endif %}
                </li>

            {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
