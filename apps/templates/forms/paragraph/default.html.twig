{% use "tmpl/form_themes.html.twig" %}
aa
<div class="row">
    {% for input in row %}
        <div class="col-12">
            {{ form_row(input) }}
        </div>
    {% endfor %}
    {% if allow_delete %}
        <div class="text-center col-12">
            {{ block('collection_delete') }}
        </div>
    {% endif %}
</div>