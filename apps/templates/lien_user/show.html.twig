{% extends 'base.html.twig' %}

{% block title %}LienUser{% endblock %}

{% block body %}
    <h1>LienUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lien_user.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lien_user.name }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ lien_user.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lien_user_index') }}">back to list</a>

    <a href="{{ path('lien_user_edit', {'id': lien_user.id}) }}">edit</a>

    {{ include('lien_user/_delete_form.html.twig') }}
{% endblock %}
