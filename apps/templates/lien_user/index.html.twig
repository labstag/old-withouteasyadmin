{% extends 'base.html.twig' %}

{% block title %}LienUser index{% endblock %}

{% block body %}
    <h1>LienUser index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Adresse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lien_user in lien_users %}
            <tr>
                <td>{{ lien_user.id }}</td>
                <td>{{ lien_user.name }}</td>
                <td>{{ lien_user.adresse }}</td>
                <td>
                    <a href="{{ path('lien_user_show', {'id': lien_user.id}) }}">show</a>
                    <a href="{{ path('lien_user_edit', {'id': lien_user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('lien_user_new') }}">Create new</a>
{% endblock %}
