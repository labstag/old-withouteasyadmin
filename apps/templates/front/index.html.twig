{% extends 'front.html.twig' %}
{% block body %}
    {% for row, block in blocks %}
        <{{ row }}>
            {% for html in block %}
                {{ html.content|raw }}
            {% endfor %}
        </{{ row }}>
    {% endfor %}
{% endblock %}
